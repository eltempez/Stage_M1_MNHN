######################
### DATA TO MODIFY ###
######################
##### Input files #####
# fastq files to analyse (put '#' in front of r2 if unpaired)
r1: "/mnt/beegfs/etempez/remapping/cinetique_illu_test/DON_test_R1.fastq.gz"
r2: "/mnt/beegfs/etempez/remapping/cinetique_illu_test/DON_test_R2.fastq.gz"
# read length
read_lg: 151

# species folder : should only contain <speciesname>.fa or <speciesname>.fasta file for each species
folder_species: "/mnt/beegfs/etempez/remapping/genome/"


##### Output infos #####
# output folder (leave empty quotes if you want the output in the folder from which you are launching the snakemake command line)
output_folder: "/mnt/beegfs/etempez/remapping/outputs/"
# build name : name of the metagenome version
build_name: "librairie_finale"


##### Configuration #####
# name of folder with all .yml files + the metagenome_ncbi_id.txt file
config_folder: "/trinity/home/etempez/remapping_scripts/config_files/"


##### Bowtie and Kraken #####
# should we use Bowtie2 for mapping ? True or False (if False, Kraken2 will be used)
use_bowtie: False
# folder containing the kraken librairy for rescue of unmapped reads
kraken_library: "/mnt/beegfs/etempez/remapping/kraken_libraries/k2protocol_db/"
#kraken_library: "/mnt/beegfs/etempez/remapping/kraken_libraries/kraken_standard/"